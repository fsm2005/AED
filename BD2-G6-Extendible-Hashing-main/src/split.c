#include <stdio.h>
#include "../include/split.h"

void dividir_bucket(struct Directorio* dir, int indice_bucket) {
    (void)dir;            // evita warning de parametro no usado
    (void)indice_bucket;  // evita warning de parametro no usado

    printf("[STUB] dividir_bucket() llamado para indice %d - falta implementar\n", indice_bucket);
}